Extracted zlib into another directory, copied the "makefile.w32" from the MSDOS directory into the
root zlib directory. Ran "nmake -f makefile.w32" on it.

Then, copied the zlib.lib, zlib.h and zconf.h files into the BWUnzip directory.